# Dolphin Frontend - Deployment env template for Netlify
# Copy the needed variables into Netlify Site → Site settings → Environment variables

VUE_APP_API_BASE_URL=https://<your-backend>.onrender.com
VUE_APP_NAME=Dolphin
VUE_APP_DEBUG=false
VUE_APP_STRIPE_PUBLIC_KEY=pk_test_xxx

# Notes:
# - All VUE_APP_* variables are included at build time. Update in Netlify UI and trigger a redeploy.
# - Do NOT put secrets that must remain private into frontend code.

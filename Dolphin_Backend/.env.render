# Render Environment Variables Template
# Copy these to your Render Dashboard → Service → Environment

# ===========================================
# REQUIRED: Application Settings
# ===========================================
APP_NAME=Dolphin
APP_ENV=production
APP_DEBUG=false
APP_KEY=                                    # Generate: php artisan key:generate --show
APP_URL=                                    # Your Render backend URL (e.g., https://dolphin-backend.onrender.com)

# ===========================================
# REQUIRED: Frontend URLs (CORS)
# ===========================================
FRONTEND_URL=                               # Your Netlify URL (e.g., https://your-app.netlify.app)
FRONTEND_URLS=                              # Comma-separated origins (e.g., https://your-app.netlify.app,https://www.yourdomain.com)

# ===========================================
# Logging
# ===========================================
LOG_CHANNEL=errorlog                        # Use errorlog for Render
LOG_LEVEL=info
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null

# ===========================================
# Database (Auto-configured by Render)
# ===========================================
DB_CONNECTION=pgsql                         # PostgreSQL on Render
DB_HOST=                                    # Auto: from render.yaml
DB_PORT=                                    # Auto: from render.yaml
DB_DATABASE=                                # Auto: from render.yaml
DB_USERNAME=                                # Auto: from render.yaml
DB_PASSWORD=                                # Auto: from render.yaml

# ===========================================
# Session & Cache
# ===========================================
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

CACHE_STORE=database
QUEUE_CONNECTION=database
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local

# ===========================================
# REQUIRED: Mail Configuration
# ===========================================
MAIL_MAILER=smtp                            # Use your SMTP provider
MAIL_HOST=                                  # e.g., smtp.gmail.com, smtp.sendgrid.net
MAIL_PORT=587                               # Usually 587 for TLS or 465 for SSL
MAIL_USERNAME=                              # Your email/username
MAIL_PASSWORD=                              # Your email password or API key
MAIL_ENCRYPTION=tls                         # tls or ssl
MAIL_FROM_ADDRESS=noreply@dolphin.app      # Sender email
MAIL_FROM_NAME=Dolphin                      # Sender name

# ===========================================
# REQUIRED: Stripe Payment Integration
# ===========================================
STRIPE_KEY=                                 # Your production publishable key (pk_live_...)
STRIPE_SECRET=                              # Your production secret key (sk_live_...)
STRIPE_WEBHOOK_SECRET=                      # Webhook signing secret from Stripe Dashboard

# ===========================================
# REQUIRED: Gemini AI Integration
# ===========================================
GEMINI_API_KEY=                             # Your Google Gemini API key
GEMINI_MODEL=gemini-pro                     # Model to use
GEMINI_PLAN=student                         # student or professional

# ===========================================
# Application Settings
# ===========================================
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
APP_MAINTENANCE_DRIVER=file
PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

# ===========================================
# Redis (Optional - for better performance)
# ===========================================
# Uncomment if you add Redis to your Render setup
# REDIS_CLIENT=phpredis
# REDIS_HOST=
# REDIS_PASSWORD=
# REDIS_PORT=6379
# CACHE_STORE=redis
# QUEUE_CONNECTION=redis
# SESSION_DRIVER=redis

# ===========================================
# AWS S3 (Optional - for file storage)
# ===========================================
# Uncomment if you want to use S3 for file storage
# FILESYSTEM_DISK=s3
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=
# AWS_USE_PATH_STYLE_ENDPOINT=false

# ===========================================
# Notes for Render Configuration
# ===========================================
#
# 1. APP_KEY: Generate locally with: php artisan key:generate --show
# 2. Database vars: Auto-configured via render.yaml from dolphin-db
# 3. APP_URL: Update after Render creates your service
# 4. FRONTEND_URL(S): Must match your Netlify deployment URL(s)
# 5. Stripe keys: Use production keys (pk_live_ and sk_live_)
# 6. Mail: Configure with your SMTP provider (Gmail, SendGrid, etc.)
# 7. Gemini: Get API key from Google AI Studio
#
# After updating environment variables in Render, 
# the service will automatically redeploy.
